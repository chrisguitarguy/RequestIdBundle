language: php

php:
  - 5.6
  - 5.5
  - 7

env:
    matrix:
        - COMPOSER_FLAGS=""
        - COMPOSER_FLAGS="--prefer-lowest"


notification:
    email: false

install:
    - composer install $COMPOSER_FLAGS

script: make test
